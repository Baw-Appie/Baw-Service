extends ./includes/layout
block contents
  h3 로그인
  form(action="https://#{hostname}/auth/login" method="post")
    p Baw Service에 로그인하려면 아래 정보를 입력하세요.
    .uk-margin
        input.uk-input(name="id" type="text" placeholder="ID")
    .uk-margin
        input.uk-input(name="pass" type="password" placeholder="Password")
    p
      button.uk-button.uk-button-default.uk-width-1-1 로그인
    p 아직 계정이 없으신가요?
    p
      button.uk-button.uk-button-primary.uk-width-1-1 회원가입
    a(onclick="location.href=('https://#{hostname}/auth/google')")
      p Google 계정으로 로그인하기
    a(onclick="location.href=('https://#{hostname}/auth/kakao')")
      p 카카오 계정으로 로그인하기
